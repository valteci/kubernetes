apiVersion: v1
kind: Service
metadata:
  name: nginx-nodeport-svc
  namespace: lab
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
      # nodePort: 30080   # opcional; se omitir, o Kubernetes escolhe uma porta
